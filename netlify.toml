[build]
  base = "frontend"
  command = "npm install --legacy-peer-deps && react-scripts build"
  publish = "build"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200