[build]
  base = "frontend"
  command = "npm install --legacy-peer-deps && react-scripts build"
  publish = "frontend/build"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200